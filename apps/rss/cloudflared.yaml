---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared
  namespace: rss
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudflared
  template:
    metadata:
      labels:
        app: cloudflared
    spec:
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:latest
          args:
            - tunnel
            - --no-autoupdate # Let ArgoCD/Kubernetes manage the updates
            - run
          env:
            - name: TUNNEL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: rss-secrets
                  key: TUNNEL_TOKEN